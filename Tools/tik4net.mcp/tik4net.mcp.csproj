<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <AssemblyName>tik4net.mcp</AssemblyName>
    <RootNamespace>tik4net.mcp</RootNamespace>

    <!-- Pack as a .NET global/local tool: `dotnet tool install -g tik4net.mcp` → `tik4net-mcp` on PATH. -->
    <PackAsTool>true</PackAsTool>
    <ToolCommandName>tik4net-mcp</ToolCommandName>
    <PackageId>tik4net.mcp</PackageId>
    <VersionPrefix>4.0.0</VersionPrefix>
    <AssemblyVersion>$(VersionPrefix).0</AssemblyVersion>
    <FileVersion>$(VersionPrefix).0</FileVersion>
    <Authors>Daniel Frantik</Authors>
    <Copyright>Copyright (C) Daniel Frantik 2026</Copyright>
    <Description>MCP (Model Context Protocol) server exposing MikroTik routers via tik4net. mikrotik_call runs any RouterOS command over every tik4net transport (API/REST/Telnet/SSH/MAC-Telnet/WinBox CLI/WinBox native, including the MAC-layer variants) with an optional per-transport raw protocol trace; mikrotik_cli_complete enumerates a menu's writable fields via RouterOS Tab-completion; mikrotik_discover finds routers on the local segment via MNDP, without host or credentials.</Description>
    <PackageTags>Mikrotik MCP ModelContextProtocol AI Claude RouterOS API CLI WinBox</PackageTags>
    <PackageProjectUrl>https://github.com/danikf/tik4net/wiki/MCP-server</PackageProjectUrl>
    <RepositoryUrl>https://github.com/danikf/tik4net</RepositoryUrl>
    <RepositoryType>git</RepositoryType>
    <PackageLicenseFile>LICENSE</PackageLicenseFile>
    <PackageReadmeFile>README.md</PackageReadmeFile>
    <PackageOutputPath>../../Build</PackageOutputPath>
  </PropertyGroup>

  <ItemGroup>
    <None Include="../../LICENSE" Pack="true" PackagePath="\" />
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="ModelContextProtocol" Version="0.3.*" />
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="8.*" />
    <PackageReference Include="Microsoft.Extensions.Logging.Console" Version="8.*" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\tik4net\tik4net.csproj" />
    <!-- The SSH transport lives in a satellite package (Renci.SshNet); referenced so the server can
         offer the Ssh transport. This project packs as a .NET tool, so the referenced assemblies are
         bundled into the tool payload rather than becoming package dependencies. -->
    <ProjectReference Include="..\..\tik4net.ssh\tik4net.ssh.csproj" />
  </ItemGroup>
</Project>

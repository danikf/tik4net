From 3f1779bc79277d8ff763f896d8c3aa961cab6b0b Mon Sep 17 00:00:00 2001
From: Claude <noreply@anthropic.com>
Date: Mon, 13 Jul 2026 09:54:54 +0000
Subject: [PATCH] Wiki audit: fix invalid info, broken samples and markdown,
 dedupe Home

Verified every page against the current source code:

- LoadById/LoadByName throw TikNoSuchItemException (were documented as returning null)
- REST SafeMode* throws TikConnectionCapabilityNotSupportedException (not NotSupportedException)
- WinboxNative reports Crud|Listen|SafeMode; M2 error fallback is TikCommandTrapException
- CLI transport pages (MacTelnet, WinboxCli, WinboxCliMac) now show SafeMode|RawCommand capabilities
- History: raw pass-through API is CreateRawCommand (not ExecuteRaw)
- login-versions: unification happened in tik4net 3.5.0, not 'RouterOS 3.5.0'
- Testing pages: entity samples now use WithId/WithValue (Id setters are private, old samples
  did not compile); ToolTorch fields are Tx/Rx; Save-update section rewritten for 4.x change tracking
- Removed stray tool-call artifact at end of Getting-started
- Fixed ##Heading-without-space markdown (rendered as literal text, broke anchors)
- Normalized Connection./connection. casing in samples; typos; TikListMerge missing brace
- Home: removed duplicate Documentation link list
- MCP-server: clarified one-shot commands and unsupported transports (Ssh, WinboxNativeMac)
- MNDP: completed TikInstanceDescriptor field table

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01JS3W4HV6uDZiSDtZ4Tajkv
---
 ADO.NET-like-API.md                  |  8 +++---
 CRUD-examples-for-all-APIs.md        | 14 +++++-----
 Communication-debugging-&-testing.md |  4 +--
 Getting-started.md                   |  2 --
 High-level-API-CRUD.md               |  4 +--
 High-level-API-advanced.md           | 14 +++++-----
 High-level-API-entities.md           |  4 +--
 High-level-API-reading-data.md       | 31 ++++++++++-----------
 High-level-API-with-O-R-mapper.md    |  4 +--
 History.md                           |  2 +-
 Home.md                              | 11 +-------
 How-to-use-tik4net-library.md        | 12 ++++-----
 Low-level-API.md                     |  4 +--
 MAC-Telnet-connection.md             | 11 +++++---
 MCP-server.md                        |  6 +++--
 MNDP.md                              |  5 +++-
 Safe-Mode.md                         |  7 ++---
 Testing-high-level-API.md            | 40 +++++++++++++++-------------
 TikListMerge.md                      |  5 ++--
 WinBox-CLI-MAC-connection.md         | 14 ++++++----
 WinBox-CLI-connection.md             | 14 ++++++----
 WinBox-Native-MAC-connection.md      |  2 +-
 WinBox-Native-connection.md          | 13 +++++----
 login-versions.md                    |  2 +-
 24 files changed, 124 insertions(+), 109 deletions(-)

diff --git a/ADO.NET-like-API.md b/ADO.NET-like-API.md
index 7aeff36..7e3ef28 100644
--- a/ADO.NET-like-API.md
+++ b/ADO.NET-like-API.md
@@ -11,14 +11,14 @@ string identity = command.ExecuteScalar();
 Console.WriteLine("Router identity: {0}", identity);
 ```
 
-##How to create command
+## How to create command
 * connection.**CreateCommand**
 * connection.**CreateCommandAndParameters**
 ```cs
 ITikCommand command = connection.CreateCommand("/system/identity/print");
 ```
 
-##Command methods
+## Command methods
 * **ExecuteNonQuery** - executes command on router and ensures that the operation was successful.
 * **ExecuteScalar** - executes command on router and ensures that operation returns one value (=ret parameter), which is returned as result.
 * **ExecuteSingleRow** - executes command on router and ensures that operation returns exactly one row (1x !re and 1x !done) as result.
@@ -26,7 +26,7 @@ ITikCommand command = connection.CreateCommand("/system/identity/print");
 * **ExecuteListWithDuration** - Executes command on router and returns all result sentences (all !re sentences) which are returned during given duration. After this period, command is automatically stopped via Cancel command.
 * **ExecuteAsync** - executes command on router asynchronously. Response is returned via callback when it is read from mikrotik (for tag, which has been dynamically assigned). Running command should be later canceled via Cancel method.
 
-##How to use parameters
+## How to use parameters
 Parameters are similar to DB ones. Instance of **ITikCommandParameter** should be created via connection.**CreateParameter** method.
 ```cs
 var cmd = connection.CreateCommand("/log/" + logLevelCommandSufix,
@@ -34,7 +34,7 @@ var cmd = connection.CreateCommand("/log/" + logLevelCommandSufix,
 cmd.ExecuteNonQuery();
 ```
 
-##Support for asynchronous commands
+## Support for asynchronous commands
 Sometimes you need to perform a command for a longer time and read response messages. A typical example is using torch. For this reason the command.ExecuteAsync method is provided. It handles all **.tag** magic (see mikrotik wiki) in the background.
 
 Async command support methods:
diff --git a/CRUD-examples-for-all-APIs.md b/CRUD-examples-for-all-APIs.md
index 3b203d3..9965f05 100644
--- a/CRUD-examples-for-all-APIs.md
+++ b/CRUD-examples-for-all-APIs.md
@@ -54,7 +54,7 @@ Load 192.168.1.1/24 IP address details via [Highlevel O/R mapper like API](High-
 ```cs
 using tik4net.Objects;
 // ...
-var ipAddress = connection.LoadSingle<Objects.Ip.IpAddress>(Connection.CreateParameter("address", "192.168.1.1/24"));
+var ipAddress = connection.LoadSingle<Objects.Ip.IpAddress>(connection.CreateParameter("address", "192.168.1.1/24"));
 var itemId = ipAddress.Id;
 var itemInterface = ipAddress.Interface;
 ```
@@ -84,7 +84,7 @@ using tik4net.Objects;
 // expectation: we know item .id in format *A
 var address = connection.LoadById<Objects.Ip.IpAddress>(id);
 address.Comment = "test comment";
-Connection.Save(address);
+connection.Save(address);
 // throws exception when there is a problem
 ```
 
@@ -92,7 +92,7 @@ Connection.Save(address);
 Delete IP address with given .id via [Low-level API](Low-level-API)
 ```cs
 // expectation: we know item .id in format *A
-var response = Connection.CallCommandSync("/ip/address/remove", $"=.id={id}");
+var response = connection.CallCommandSync("/ip/address/remove", $"=.id={id}");
 // should return single ITikDoneSentence sentence, returns ITikTrapSentence sentence when there is a problem
 ```
 
@@ -110,7 +110,7 @@ using tik4net.Objects;
 // ...
 // expectation: we know item .id in format *A
 var ipAddress = connection.LoadById<Objects.Ip.IpAddress>(id);
-Connection.Delete(ipAddress);
+connection.Delete(ipAddress);
 // throws exception when there is a problem
 ```
 
@@ -119,7 +119,7 @@ Delete IP address 192.168.1.1/24 via [Low-level API](Low-level-API)
 ```cs
 var findResponse = connection.CallCommandSync("/ip/address/print", "?=address=192.168.1.1/24");
 var id = findResponse.OfType<ITikReSentence>().Single().GetId();
-var deleteResponse = Connection.CallCommandSync("/ip/address/remove", $"=.id={id}");
+var deleteResponse = connection.CallCommandSync("/ip/address/remove", $"=.id={id}");
 // should return single ITikDoneSentence sentence, returns ITikTrapSentence sentence when there is a problem
 ```
 
@@ -136,8 +136,8 @@ Delete IP address 192.168.1.1/24 via [Highlevel O/R mapper like API](High-level-
 ```cs
 using tik4net.Objects;
 // ...
-var ipAddress = connection.LoadSingle<Objects.Ip.IpAddress>(Connection.CreateParameter("address", "192.168.1.1/24"));
-Connection.Delete(ipAddress);
+var ipAddress = connection.LoadSingle<Objects.Ip.IpAddress>(connection.CreateParameter("address", "192.168.1.1/24"));
+connection.Delete(ipAddress);
 // throws exception when there is a problem
 ```
 
diff --git a/Communication-debugging-&-testing.md b/Communication-debugging-&-testing.md
index 4528d5a..5366b92 100644
--- a/Communication-debugging-&-testing.md
+++ b/Communication-debugging-&-testing.md
@@ -11,7 +11,7 @@ the O/R mapper — runs through real code.
 Quick example:
 ```cs
 var conn = new TikFakeConnection()
-    .WithEntities(new IpAddress { Id = "*1", Address = "10.0.0.1/24", Interface = "ether1" })
+    .WithEntities(new IpAddress { Address = "10.0.0.1/24", Interface = "ether1" }.WithId("*1"))
     .WithScalarResponse(rows => rows.First() == "/ip/address/add", "*2")
     .WithNonQuery(rows => rows.First() == "/ip/address/set");
 
@@ -28,7 +28,7 @@ Since v 3.5 - complete communication is written to the Output window.
 If you want to see what is going to the router or see router response, you can hook **OnWriteRow** and **OnReadRow** on **ITikConnection** object.
 ```cs
         // ....
-        Connection.OnWriteRow += Connection_OnWriteRow;
+        connection.OnWriteRow += Connection_OnWriteRow;
         // ....
 
         private void Connection_OnWriteRow(object sender, TikConnectionCommCallbackEventArgs e)
diff --git a/Getting-started.md b/Getting-started.md
index 6575642..b8675af 100644
--- a/Getting-started.md
+++ b/Getting-started.md
@@ -137,5 +137,3 @@ using (ITikConnection connection = ConnectionFactory.OpenConnection(TikConnectio
 * [Exception handling](Exception-handling) — the error model and exception tree.
 * [Communication debugging & testing](Communication-debugging-&-testing) — `tik4net.testing` and
   protocol tracing.
-</content>
-</invoke>
diff --git a/High-level-API-CRUD.md b/High-level-API-CRUD.md
index cc6d5da..f9272ea 100644
--- a/High-level-API-CRUD.md
+++ b/High-level-API-CRUD.md
@@ -33,13 +33,13 @@ Advanced version of **Save** which works with a whole list of entities. See [adv
 ### Delete
 Deletes entity (.id is the key) on mikrotik router.
 ```cs
-var loadedFilter = Connection.LoadById<BridgeFilter>(filter.Id);
+var loadedFilter = connection.LoadById<BridgeFilter>(filter.Id);
 connection.Delete<BridgeFilter>(loadedFilter);
 ```
 ### DeleteAll
 Deletes all entities of given type on mikrotik router.
 ```cs
-int deletedCnt = Connection.DeleteAll<HotspotUser>();
+int deletedCnt = connection.DeleteAll<HotspotUser>();
 ```
 ### Move
 Moves given entityToMove before given entityToMoveBefore. REMARKS: only some entities supports ordering.
diff --git a/High-level-API-advanced.md b/High-level-API-advanced.md
index 5f12178..e023dd3 100644
--- a/High-level-API-advanced.md
+++ b/High-level-API-advanced.md
@@ -5,36 +5,36 @@ Trivial usage of **LoadAll** and **Save** methods is not always sufficient. This
 * **Asynchronous loading**
 * **Merging lists**
 
-##Loading with filter
+## Loading with filter
 Printing all connections from 192.168.1.2 address:
 ```cs
 var command = connection.CreateCommandAndParameters("/ip/firewall/connection/print",
      "src-address", "192.168.3.103");
 var list = command.ExecuteList();
 ```
-###Loading with custom advanced filter
+### Loading with custom advanced filter
 Usage of custom filter (search for **ether** OR **wlan** interface). For details about filtering see [mikrotik API wiki page](http://wiki.mikrotik.com/wiki/Manual:API#Queries).
 ```cs
-var cmd = Connection.CreateCommandAndParameters(@"/interface/print
+var cmd = connection.CreateCommandAndParameters(@"/interface/print
       ?type=ether
       ?type=wlan
       ?#|");
 var list = cmd.ExecuteList();
 ```
-###Saving list (saving differences)
+### Saving list (saving differences)
 The main idea is to remember unmodified list, do some changes and save only differences.
 ```cs
 var existingAddressList = connection.LoadList<FirewallAddressList>().ToList(); // imagine that returns 10 rows
 var listClonedBackup = existingAddressList.CloneEntityList(); //creates clone of all entities in list
 
-existingAdressList.RemoveAt(0); //remove first item
+existingAddressList.RemoveAt(0); //remove first item
 existingAddressList.Add(new FirewallAddressList() {Address="192.168.1.1", List="MY-LIST" }); //add new one
 existingAddressList[0].Comment = "New comment";
 
 //save differences into mikrotik  (existingAddressList=modified, listClonedBackup=unmodified)
 connection.SaveListDifferences(existingAddressList, listClonedBackup); //performs 1 delete, 1 insert and 1 update
 ```
-###Asynchronous loading
+### Asynchronous loading
 The main reason is that sometimes you cannot wait for a result (it never ends). For example with torch, scan, sniffer, ...
 Then you have to start listening, handle responses asynchronously, and finally cancel the running operation.
 
@@ -52,5 +52,5 @@ Console.WriteLine("Press ENTER"); //torch command is running in different thread
 Console.ReadLine();
 torchCmd.CancelAndJoin(); //stop torch thread
 ```
-###Merging lists
+### Merging lists
 The main idea is that you can prepare expected state on mikrotik router (for example from database) and than you want to somehow update mikrotik to be on the same state. But you want to do it with minimal number of operations. There merge support could help. See [this wiki page](TikListMerge) for details.
diff --git a/High-level-API-entities.md b/High-level-API-entities.md
index 87c6bb1..12628f8 100644
--- a/High-level-API-entities.md
+++ b/High-level-API-entities.md
@@ -17,7 +17,7 @@ public class Interface
   public string Id { get; private set; }
   public string Name { get; set; }
   public string Type { get; set; }
-  public string Mtu { get; set; }}
+  public string Mtu { get; set; }
 }
 ```
 
@@ -42,4 +42,4 @@ var list = connection.LoadAll<Interface>();
 ```
 See [this wiki page](High-level-API-reading-data) for details.
 
-*NOTE:* See [How to create custom entity](High-level-API-custom-entities) if you are missing some entity in tik4net projekt. You can clone the github repository, create your own entity and share it via "pull request" feature with others.
\ No newline at end of file
+*NOTE:* See [How to create custom entity](High-level-API-custom-entities) if you are missing some entity in tik4net project. You can clone the github repository, create your own entity and share it via "pull request" feature with others.
\ No newline at end of file
diff --git a/High-level-API-reading-data.md b/High-level-API-reading-data.md
index 7bc5323..135f1e1 100644
--- a/High-level-API-reading-data.md
+++ b/High-level-API-reading-data.md
@@ -2,9 +2,10 @@ All data-read methods are implemented as extension methods of **ITikConnection**
 
 
 * connection.**LoadAll<TEntity>**: Loads all entity list without filtering.
-* connection.**LoadSingle<TEntity>**: Loads exactly one row (optionally with filter).
-* connection.**LoadSingleOrDefault<TEntity>**: Loads one row or nothing.
-* connection.**LoadById<TEntity>**: Loads entity with specified id. Returns null if not found.
+* connection.**LoadSingle<TEntity>**: Loads exactly one row (optionally with filter). Throws when zero or more than one row matches.
+* connection.**LoadSingleOrDefault<TEntity>**: Loads one row or returns null when nothing matches.
+* connection.**LoadById<TEntity>**: Loads entity with specified id. Throws `TikNoSuchItemException` if not found.
+* connection.**LoadByName<TEntity>**: Loads entity with specified `name` field value. Throws `TikNoSuchItemException` if not found.
 * connection.**LoadList<TEntity>**: Loads entity list. Could be filtered with filterParameters.
 * command.**LoadList<TEntity>**: --dtto--
 * connection.**LoadWithDuration<TEntity>**: Calls command and reads all returned rows for given period.
@@ -14,37 +15,37 @@ All data-read methods are implemented as extension methods of **ITikConnection**
 * connection.**LoadListenAsync<TEntity>**: Starts real-time monitoring of an entity list via the RouterOS `/listen` command. Streams change notifications until cancelled.
 * command.**LoadListenAsync<TEntity>**: -dtto-
 
-###LoadAll<TEntity>
+### LoadAll<TEntity>
 Alias to LoadList<TEntity> without filter. You can filter list via LINQ on client side.
 ```cs
 // load all QT entities
-var queues = connection.LoadAll<QueueTree>()
+var queues = connection.LoadAll<QueueTree>();
 ```
-###LoadSingle<TEntity>
+### LoadSingle<TEntity>
 Alias to LoadList<TEntity> optionally with filter, ensures that the result contains exactly one row. Throws an exception if the command returns zero or more than one entity.
 ```cs
 // there should be exactly one row in response for /system/resource/print
 var sysRes = connection.LoadSingle<SystemResource>();
 ```
-###LoadSingleOrDefault
+### LoadSingleOrDefault
 Similar to LoadSingle but returns null if row is not found.
 ```cs
 // try to find mangle by its comment
 var startMangle = connection.LoadSingleOrDefault<FirewallMangle>(
     connection.CreateParameter("comment", "MY COMMENT"));
 ```
-###LoadById
-Loads entity with specified id. Returns null if not found.
+### LoadById
+Loads entity with specified id. Throws `TikNoSuchItemException` if not found (use `LoadSingleOrDefault` with an `.id` filter when you prefer a null result).
 ```cs
 //from unittest
 connection.Save(filter);
 
-// verify that all values were realy written to mikrotik router
-var loadedFilter = Connection.LoadById<BridgeFilter>(filter.Id);
+// verify that all values were really written to mikrotik router
+var loadedFilter = connection.LoadById<BridgeFilter>(filter.Id);
 Assert.IsNotNull(loadedFilter);
 Assert.AreEqual(filter.Chain, loadedFilter.Chain);
 ```
-###LoadList
+### LoadList
 Loads entity list. Could be filtered with filterParameters.
 ```cs
 // load all log entries
@@ -54,11 +55,11 @@ foreach (Log log in logs)
     Console.WriteLine("{0}[{1}]: {2}", log.Time, log.Topics, log.Message);
 }
 ```
-###LoadWithDuration
+### LoadWithDuration
 Calls command and reads all returned rows for given durationSec period.
 After this period calls cancel on the MikroTik router and returns all loaded rows.
 Throws exception if any 'trap' row occurs.
-###LoadAsync
+### LoadAsync
 Calls command and starts a background reading thread. After that returns control to the calling thread.
 All read rows are returned as callbacks (onLoadItemCallback, onExceptionCallback) from the loading thread.
 
@@ -80,7 +81,7 @@ var cmd = connection.LoadAsync<ToolTorch>(
 
    cmd.Cancel();
 ```
-###LoadListenAsync
+### LoadListenAsync
 Starts real-time monitoring of an entity list via the RouterOS `/listen` command.
 Unlike `LoadAsync` (which uses `/print`), `/listen` sends `!re` sentences only when an item changes — it never sends `!done`.
 Useful for watching interface state, IP address changes, routing table updates, etc.
diff --git a/High-level-API-with-O-R-mapper.md b/High-level-API-with-O-R-mapper.md
index efb008e..dd0aa2f 100644
--- a/High-level-API-with-O-R-mapper.md
+++ b/High-level-API-with-O-R-mapper.md
@@ -11,7 +11,7 @@ using tik4net;
 using tik4net.Objects;
 // ...
 connection = ConnectionFactory.OpenConnection(TikConnectionType.Api, HOST, USER, PASS);
-var interfaces = Connection.LoadAll<Interface>();
+var interfaces = connection.LoadAll<Interface>();
 ```
 
 ## Using High-level API
@@ -37,7 +37,7 @@ private static void ChangeCommentOnInterfaceWithName(ITikConnection connection,
 
 ```cs
 // SCRIPT equivalent: interface set comment=test [/interface find default-name=ether1]
-private static void ChangeCommentOnInterfaceWithDefraultName(ITikConnection connection, string defaultName, string newComment)
+private static void ChangeCommentOnInterfaceWithDefaultName(ITikConnection connection, string defaultName, string newComment)
 {
   // Load by default-name (filter)
   var iface = connection.LoadSingle<Objects.Interface.Interface>(connection.CreateParameter("default-name", defaultName));
diff --git a/History.md b/History.md
index 4ff4141..c77f98c 100644
--- a/History.md
+++ b/History.md
@@ -21,7 +21,7 @@ All new transports implement the same `ITikConnection` / `LoadAll<T>()` / `Save<
 - **Streaming monitors** — `ExecuteAsync` / `LoadListenAsync` for live data (WinBox Native; listen poll+diff on CLI transports)
 - **`TikConnectionSetup`** — unified connection configuration builder
 - **Connection capability model** — `connection.Supports(TikConnectionCapability.Listen)` etc.
-- **Raw command pass-through** — `ExecuteRaw` for commands outside the O/R mapper
+- **Raw command pass-through** — `CreateRawCommand` for commands outside the O/R mapper
 - **WinBox GUI-name addressing** — address entities by WinBox display name on `WinboxNative`
 - **MCP server tool** (`tik4net.mcp`) — run any command over any transport from an AI assistant
 
diff --git a/Home.md b/Home.md
index 180e14e..e51971f 100644
--- a/Home.md
+++ b/Home.md
@@ -97,6 +97,7 @@ using (ITikConnection connection = ConnectionFactory.CreateConnection(TikConnect
 * [ADO.NET-like API](ADO.NET-like-API) — commands, parameters, scalars/lists.
 * [Low-level API](Low-level-API) — raw sentences, like other MikroTik libraries.
 * [VisualBasic example](VB-trivial-example) · [Exception handling](Exception-handling) · [History / changelog](History)
+* [Communication debugging & testing](Communication-debugging-&-testing) — protocol tracing and unit testing without a router.
 * **[4.x Roadmap](Roadmap-4x)** — what's planned for future 4.x releases.
 
 ## Choosing the three API levels
@@ -146,15 +147,5 @@ All transports sit behind the same `ITikConnection` interface — see
 
 ---
 
-## Documentation
-
-* [Getting started](Getting-started) — step-by-step first project
-* [How to use tik4net](How-to-use-tik4net-library)
-* [CRUD examples for all APIs](CRUD-examples-for-all-APIs)
-* [Exception handling](Exception-handling) — error handling and the full exception tree
-* [VisualBasic trivial example](VB-trivial-example)
-* [History / changelog](History)
-* **[4.x Roadmap](Roadmap-4x)**
-
 For the MikroTik protocol itself, see the
 [MikroTik API manual](https://help.mikrotik.com/docs/display/ROS/API).
diff --git a/How-to-use-tik4net-library.md b/How-to-use-tik4net-library.md
index 8028a40..67581ad 100644
--- a/How-to-use-tik4net-library.md
+++ b/How-to-use-tik4net-library.md
@@ -18,16 +18,16 @@ When using tik4Net library, you should consider which API you would like to use:
 ## Low-level API
 * **[Low-level API](Low-level-API)**: first option is to use low-level API like other mikrotik libraries do. It allows you to send API commands to mikrotik router and read response to these command.
 ```cs
-// expectes opened connection - see example above
+// expects opened connection - see example above
 string[] command = new string[] { "/interface/print" };
-IEnumerable<ITikSentence> result = Connection.CallCommandSync(command);
-// returns list of respose sentences
+IEnumerable<ITikSentence> result = connection.CallCommandSync(command);
+// returns list of response sentences
 ```
 
 ## ADO.NET like API
-* **[ADO.NET like API](ADO.NET-like-API)**: second option is to use ADO.NET like API. This API tries to be very simmilar to database API used in .NET world. It allows you to create ITikCommand commands, use parameters and other features like ADO.NET does.
+* **[ADO.NET like API](ADO.NET-like-API)**: second option is to use ADO.NET like API. This API tries to be very similar to database API used in .NET world. It allows you to create ITikCommand commands, use parameters and other features like ADO.NET does.
 ```cs
-// expectes opened connection - see example above
+// expects opened connection - see example above
 ITikCommand cmd = connection.CreateCommand("/system/identity/print");
 string identity = cmd.ExecuteScalar();
 Console.WriteLine("Identity: " + identity);
@@ -37,7 +37,7 @@ Console.WriteLine("Identity: " + identity);
 ```cs
 using tik4net;
 using tik4net.Objects;
-// expectes opened connection - see example above
+// expects opened connection - see example above
 var interfaces = connection.LoadAll<Interface>();
 ```
 ## Safe Mode (rollback protection)
diff --git a/Low-level-API.md b/Low-level-API.md
index ff04255..acb7ceb 100644
--- a/Low-level-API.md
+++ b/Low-level-API.md
@@ -7,7 +7,7 @@ using tik4net;
 // ...
 connection = ConnectionFactory.OpenConnection(TikConnectionType.Api, HOST, USER, PASS);
 string[] command = new string[] { "/interface/print" };
-IEnumerable<ITikSentence> result = Connection.CallCommandSync(command);
+IEnumerable<ITikSentence> result = connection.CallCommandSync(command);
 // returns list of response sentences
 ```
 Response sentence is object with list of response words (like properties of loaded object). See ITikSentence.Words dictionary.
@@ -49,7 +49,7 @@ using tik4net;
 connection = ConnectionFactory.OpenConnection(TikConnectionType.Api, HOST, USER, PASS);
 
 string[] command = new string[] { "/interface/print" };
-IEnumerable<ITikSentence> result = Connection.CallCommandSync(command);
+IEnumerable<ITikSentence> result = connection.CallCommandSync(command);
 
 foreach(ITikSentence sentence in result)
 {
diff --git a/MAC-Telnet-connection.md b/MAC-Telnet-connection.md
index de2a894..18cc01f 100644
--- a/MAC-Telnet-connection.md
+++ b/MAC-Telnet-connection.md
@@ -97,8 +97,10 @@ using (var conn2 = setup.CreateMacTelnetConnection(routerMac: "AA:BB:CC:DD:EE:FF
 
 ## Capability
 
-CRUD **and Listen** — MAC-Telnet reports `Crud | Listen`, like all CLI transports, so the callback-based
-async APIs work in addition to synchronous CRUD. A terminal has no server push, so the async commands are
+CRUD, **Listen** and **Safe Mode** — MAC-Telnet reports `Crud | Listen | SafeMode | RawCommand`, like all CLI
+transports, so the callback-based async APIs work in addition to synchronous CRUD, and
+[Safe Mode](Safe-Mode) / [raw CLI pass-through](Command-translation-on-non-API-transports#raw-command-pass-through-createrawcommand)
+are available. A terminal has no server push, so the async commands are
 emulated by **polling** a one-shot snapshot on a background worker:
 
 | Call shape | Example | How it works |
@@ -109,8 +111,9 @@ emulated by **polling** a one-shot snapshot on a background worker:
 | **Async list** | `cmd.LoadAsync<Interface>(…)` on a `/print` | runs the read on a background thread, emits each row, completes |
 
 ```cs
-bool hasCrud   = conn.Supports(TikConnectionCapability.Crud);   // true
-bool hasListen = conn.Supports(TikConnectionCapability.Listen); // true
+bool hasCrud     = conn.Supports(TikConnectionCapability.Crud);     // true
+bool hasListen   = conn.Supports(TikConnectionCapability.Listen);   // true
+bool hasSafeMode = conn.Supports(TikConnectionCapability.SafeMode); // true
 ```
 
 `TikConnectionCapability.Streaming` (`ExecuteListWithDuration`) is **not** reported — use the API transport
diff --git a/MCP-server.md b/MCP-server.md
index 8e28405..5db03df 100644
--- a/MCP-server.md
+++ b/MCP-server.md
@@ -60,8 +60,10 @@ The `parameters` array uses MikroTik API sentence words, identical on every tran
 
 ## Transports
 
-All transports accept the same `command` / `parameters`. Only `Api` / `ApiSsl` support
-Listen/Streaming; the rest are request/response only.
+All transports accept the same `command` / `parameters`. The tool runs **one-shot request/response
+commands** — Listen/streaming commands are not usable through it (use the library directly for those).
+`Ssh` (satellite package) and `WinboxNativeMac` are **not wired into the MCP tool**; every other
+`TikConnectionType` is:
 
 | `transport`    | Wire                         | Default port | Router prerequisite |
 |----------------|------------------------------|--------------|---------------------|
diff --git a/MNDP.md b/MNDP.md
index f990202..ef431c8 100644
--- a/MNDP.md
+++ b/MNDP.md
@@ -58,5 +58,8 @@ byte[] mac = MndpHelper.FindMacByHost("192.168.4.1", TimeSpan.FromSeconds(10));
 | `Version` | RouterOS version string |
 | `Platform` | Platform/board type |
 | `BoardName` | Board name |
+| `SoftwareId` | RouterOS software ID |
+| `IPv6` | IPv6 address string (when announced) |
+| `Unpack` | Unpack/compression flag announced by the router |
 | `InterfaceName` | Interface from which the MNDP reply was sent |
-| `Uptime` | Router uptime |
\ No newline at end of file
+| `Uptime` | Router uptime (`TimeSpan`) |
\ No newline at end of file
diff --git a/Safe-Mode.md b/Safe-Mode.md
index d4995b3..aa5023f 100644
--- a/Safe-Mode.md
+++ b/Safe-Mode.md
@@ -31,7 +31,7 @@ Safe mode only makes sense on a **persistent, session-oriented** channel, becaus
 | `MacTelnet` | ✅ take / release / unroll | same control keys, over the MAC-layer terminal |
 | `WinboxCli` / `WinboxCliMac` | ✅ take / release / unroll | same control keys, over the encrypted WinBox terminal |
 | `WinboxNative` / `WinboxNativeMac` | ✅ take / release | native M2 `toggleSafeMode` commands (handler `[17]`, take `0x80003` / release `0x80005`). **No in-place unroll** — to roll back, drop the connection without releasing. RouterOS 7.18+ |
-| `Rest` / `RestSsl` | ❌ | stateless — verified: `/safe-mode/take` returns OK but the change is **never rolled back** after the HTTP request ends, so there is no protection. Throws `NotSupportedException` |
+| `Rest` / `RestSsl` | ❌ | stateless — verified: `/safe-mode/take` returns OK but the change is **never rolled back** after the HTTP request ends, so there is no protection. Throws `TikConnectionCapabilityNotSupportedException` |
 
 ```cs
 if (connection.Supports(TikConnectionCapability.SafeMode))
@@ -184,8 +184,9 @@ conn.CreateCommand("/safe-mode/release").ExecuteNonQuery();
 
 * `SafeModeGet()` reflects **this connection's** state, tracked client-side (`true` after take, `false` after
   release/unroll). It does not query the router; use `/safe-mode/print` for the router-side view.
-* `SafeModeTake/Release/Unroll` throw `NotSupportedException` on transports that cannot bind safe mode to a
-  connection (`Rest`), and `SafeModeUnroll` additionally throws on `WinboxNative` (no in-place unroll there).
+* `SafeModeTake/Release/Unroll` throw `TikConnectionCapabilityNotSupportedException` on transports that cannot
+  bind safe mode to a connection (`Rest`), and `SafeModeUnroll` additionally throws `NotSupportedException` on
+  `WinboxNative` (no in-place unroll there — drop the connection to roll back).
 
 # Links
 * [How to use tik4net](How-to-use-tik4net-library)
diff --git a/Testing-high-level-API.md b/Testing-high-level-API.md
index 328cbc0..8659c8c 100644
--- a/Testing-high-level-API.md
+++ b/Testing-high-level-API.md
@@ -28,7 +28,9 @@ to set these in test setup without raw reflection boilerplate.
 
 ### WithId
 
-Convenience shortcut for `.WithValue(".id", id)`:
+Convenience shortcut for `.WithValue(".id", id)`. Entity `Id` properties have a **private setter**
+(the router owns them), so an object initializer like `new IpAddress { Id = "*1" }` does not compile —
+`WithId` is the way to fake them:
 
 ```cs
 var entry = new FirewallAddressList { List = "BLACKLIST", Address = "10.0.0.1" }
@@ -63,8 +65,8 @@ reading from a real router:
 ```cs
 var conn = new TikFakeConnection()
     .WithEntities(
-        new IpAddress { Id = "*1", Address = "10.0.0.1/24", Interface = "ether1", Disabled = false },
-        new IpAddress { Id = "*2", Address = "10.0.0.2/24", Interface = "ether2", Disabled = true  });
+        new IpAddress { Address = "10.0.0.1/24", Interface = "ether1", Disabled = false }.WithId("*1"),
+        new IpAddress { Address = "10.0.0.2/24", Interface = "ether2", Disabled = true  }.WithId("*2"));
 
 IEnumerable<IpAddress> addresses = conn.LoadAll<IpAddress>();
 
@@ -88,7 +90,7 @@ Assert.AreEqual("MyRouter", identity.Name);
 ```cs
 // LoadById calls /print with ?.id=*1 — register both /print and the filtered response
 var conn = new TikFakeConnection()
-    .WithEntities(new IpAddress { Id = "*1", Address = "10.0.0.1/24", Interface = "ether1" });
+    .WithEntities(new IpAddress { Address = "10.0.0.1/24", Interface = "ether1" }.WithId("*1"));
 
 IpAddress addr = conn.LoadById<IpAddress>("*1");
 Assert.AreEqual("*1", addr.Id);
@@ -116,18 +118,20 @@ conn.AssertWasSent("/ip/address/add");
 
 ## Save — update (existing entity)
 
-`Save` on an existing entity calls `/print` (to diff fields), then `/set`.
-Register both:
+`Save` on an entity with an id calls `/set`. In 4.x, an entity that was **loaded** via any `Load*`
+method carries a [change-tracking](Change-tracking) snapshot, so only changed fields are sent — no
+`/print` round-trip. An entity you constructed yourself (no snapshot) sends all writable fields.
+Register the entities for the load plus a `/set` handler:
 
 ```cs
-var existing = new IpAddress { Id = "*1", Address = "10.0.0.1/24", Interface = "ether1", Disabled = false };
-
 var conn = new TikFakeConnection()
-    .WithEntities(existing)                                    // /print
+    .WithEntities(
+        new IpAddress { Address = "10.0.0.1/24", Interface = "ether1", Disabled = false }.WithId("*1"))
     .WithNonQuery(rows => rows.First() == "/ip/address/set"); // /set
 
+var existing = conn.LoadById<IpAddress>("*1");   // snapshot taken here
 existing.Disabled = true;
-conn.Save(existing);
+conn.Save(existing);                              // sends only =.id= and =disabled=
 
 conn.AssertWasSent("/ip/address/set");
 conn.AssertWasSent(rows => rows.Any(r => r.Contains("disabled=yes")));
@@ -141,7 +145,7 @@ conn.AssertWasSent(rows => rows.Any(r => r.Contains("disabled=yes")));
 var conn = new TikFakeConnection()
     .WithNonQuery(rows => rows.First() == "/ip/address/remove");
 
-var addr = new IpAddress { Id = "*3", Address = "10.0.0.3/24" };
+var addr = new IpAddress { Address = "10.0.0.3/24" }.WithId("*3");
 conn.Delete(addr);
 
 conn.AssertWasSent("/ip/address/remove");
@@ -154,8 +158,8 @@ conn.AssertWasSent("/ip/address/remove");
 ```cs
 var original = new List<IpAddress>
 {
-    new IpAddress { Id = "*1", Address = "10.0.0.1/24", Interface = "ether1" },
-    new IpAddress { Id = "*2", Address = "10.0.0.2/24", Interface = "ether2" },
+    new IpAddress { Address = "10.0.0.1/24", Interface = "ether1" }.WithId("*1"),
+    new IpAddress { Address = "10.0.0.2/24", Interface = "ether2" }.WithId("*2"),
 };
 var backup = original.CloneEntityList();
 
@@ -183,11 +187,11 @@ var done = new ManualResetEventSlim(false);
 
 var conn = new TikFakeConnection()
     .WithEntities(
-        new ToolTorch { TxBps = 1000, RxBps = 500 },
-        new ToolTorch { TxBps = 2000, RxBps = 800 });
+        new ToolTorch().WithValue("tx", 1000L).WithValue("rx", 500L),
+        new ToolTorch().WithValue("tx", 2000L).WithValue("rx", 800L));
 
 ITikCommand torchCmd = conn.LoadAsync<ToolTorch>(
-    onLoadItemCallback:  item  => received.Add(item.TxBps.ToString()),
+    onLoadItemCallback:  item  => received.Add(item.Tx.ToString()),
     onExceptionCallback: error => Assert.Fail(error.Message),
     conn.CreateParameter("interface", "ether1"),
     conn.CreateParameter("port",      "any"));
@@ -207,7 +211,7 @@ When you need the fake to reflect changes made by the code under test (load →
 ```cs
 var store = new List<QueueSimple>
 {
-    new QueueSimple { Id = "*1", Name = "Q1", MaxLimit = "10M/10M" },
+    new QueueSimple { Name = "Q1", MaxLimit = "10M/10M" }.WithId("*1"),
 };
 
 var conn = new TikFakeConnection()
@@ -215,7 +219,7 @@ var conn = new TikFakeConnection()
     .WithScalarResponse(rows => rows.First() == "/queue/simple/add", _ =>
     {
         var newId = "*" + (store.Count + 1);
-        store.Add(new QueueSimple { Id = newId, Name = "Q2", MaxLimit = "5M/5M" });
+        store.Add(new QueueSimple { Name = "Q2", MaxLimit = "5M/5M" }.WithId(newId));
         return newId;
     })
     .WithNonQuery(rows => rows.First() == "/queue/simple/set");
diff --git a/TikListMerge.md b/TikListMerge.md
index c302860..947a669 100644
--- a/TikListMerge.md
+++ b/TikListMerge.md
@@ -3,12 +3,12 @@
 * Support for re-ordering collections
 * Useful for large configurations - minimum DML actions (changes in router configuration) performed
 
-##Main idea
+## Main idea
 * Prepare expected configuration in memory list
 * Load configuration from mikrotik router
 * Use TikListMerge class to handle all modifications
 
-##Trivial Example
+## Trivial Example
 * There is list in /ip/firewall/address-list named 'MY-LIST'
 * It contains large number of items
 * We have list of IPs, that should be in address-list named 'MY-LIST' (with comments)
@@ -42,5 +42,6 @@ private void UpdateMyList()
       .Field(a => a.Comment);
 
     merge.Save(); //perform DML - change mikrotik configuration
+  }
 }
 ```
\ No newline at end of file
diff --git a/WinBox-CLI-MAC-connection.md b/WinBox-CLI-MAC-connection.md
index 612cb6d..37e1da5 100644
--- a/WinBox-CLI-MAC-connection.md
+++ b/WinBox-CLI-MAC-connection.md
@@ -78,9 +78,12 @@ using (var conn = await setup.CreateWinboxCliMacConnectionAsync(routerMac: "AA:B
 
 ## Capability
 
-CRUD **and Listen** — like all CLI transports, WinBox-CLI-MAC reports `Crud | Listen`, so the callback-based
-async APIs work in addition to synchronous CRUD. A terminal has no server push, so the async commands are
-emulated by **polling** a one-shot snapshot on a background worker:
+CRUD, **Listen** and **Safe Mode** — like all CLI transports, WinBox-CLI-MAC reports
+`Crud | Listen | SafeMode | RawCommand`, so the callback-based async APIs work in addition to synchronous
+CRUD, and [Safe Mode](Safe-Mode) /
+[raw CLI pass-through](Command-translation-on-non-API-transports#raw-command-pass-through-createrawcommand)
+are available. A terminal has no server push, so the async commands are emulated by **polling** a one-shot
+snapshot on a background worker:
 
 | Call shape | Example | How it works |
 |---|---|---|
@@ -90,8 +93,9 @@ emulated by **polling** a one-shot snapshot on a background worker:
 | **Async list** | `cmd.LoadAsync<Interface>(…)` on a `/print` | runs the read on a background thread, emits each row, completes |
 
 ```cs
-bool hasCrud   = conn.Supports(TikConnectionCapability.Crud);   // true
-bool hasListen = conn.Supports(TikConnectionCapability.Listen); // true
+bool hasCrud     = conn.Supports(TikConnectionCapability.Crud);     // true
+bool hasListen   = conn.Supports(TikConnectionCapability.Listen);   // true
+bool hasSafeMode = conn.Supports(TikConnectionCapability.SafeMode); // true
 ```
 
 `TikConnectionCapability.Streaming` (`ExecuteListWithDuration`) is **not** reported — use the API transport
diff --git a/WinBox-CLI-connection.md b/WinBox-CLI-connection.md
index e32f01a..e72a04d 100644
--- a/WinBox-CLI-connection.md
+++ b/WinBox-CLI-connection.md
@@ -83,9 +83,12 @@ using (var conn2 = setup.CreateWinboxCliConnection()) { /* ... */ }
 
 ## Capability
 
-CRUD **and Listen** — WinBox CLI reports `Crud | Listen`, like all CLI transports (Telnet, MAC-Telnet,
-WinBox-CLI-MAC), so the callback-based async APIs work in addition to synchronous CRUD. A terminal has no
-server push, so the async commands are emulated by **polling** a one-shot snapshot on a background worker:
+CRUD, **Listen** and **Safe Mode** — WinBox CLI reports `Crud | Listen | SafeMode | RawCommand`, like all CLI
+transports (Telnet, MAC-Telnet, WinBox-CLI-MAC), so the callback-based async APIs work in addition to
+synchronous CRUD, and [Safe Mode](Safe-Mode) /
+[raw CLI pass-through](Command-translation-on-non-API-transports#raw-command-pass-through-createrawcommand)
+are available. A terminal has no server push, so the async commands are emulated by **polling** a one-shot
+snapshot on a background worker:
 
 | Call shape | Example | How it works |
 |---|---|---|
@@ -95,8 +98,9 @@ server push, so the async commands are emulated by **polling** a one-shot snapsh
 | **Async list** | `cmd.LoadAsync<Interface>(…)` on a `/print` | runs the read on a background thread, emits each row, completes |
 
 ```cs
-bool hasCrud   = conn.Supports(TikConnectionCapability.Crud);   // true
-bool hasListen = conn.Supports(TikConnectionCapability.Listen); // true
+bool hasCrud     = conn.Supports(TikConnectionCapability.Crud);     // true
+bool hasListen   = conn.Supports(TikConnectionCapability.Listen);   // true
+bool hasSafeMode = conn.Supports(TikConnectionCapability.SafeMode); // true
 ```
 
 `TikConnectionCapability.Streaming` (`ExecuteListWithDuration`) is **not** reported — use the API transport
diff --git a/WinBox-Native-MAC-connection.md b/WinBox-Native-MAC-connection.md
index ba7dc16..54e5cc9 100644
--- a/WinBox-Native-MAC-connection.md
+++ b/WinBox-Native-MAC-connection.md
@@ -94,7 +94,7 @@ applies here unchanged.
 
 ## Capability
 
-CRUD **and Listen** (`Crud | Listen | SafeMode`) — same as the TCP native transport. The callback-based async
+CRUD, **Listen** and **Safe Mode** (`Crud | Listen | SafeMode`) — same as the TCP native transport. The callback-based async
 APIs (`ExecuteAsync` / `LoadAsync` / `LoadListenAsync`) work and cover streaming monitors, async lists, and
 change-listen exactly as described on [WinBox Native](WinBox-Native-connection#capability). Polling over the
 MAC layer is slow (per-packet ACKs) — prefer the TCP native transport or the [SSL API](SSL-connection) for
diff --git a/WinBox-Native-connection.md b/WinBox-Native-connection.md
index 0e7ee61..d84909f 100644
--- a/WinBox-Native-connection.md
+++ b/WinBox-Native-connection.md
@@ -204,16 +204,19 @@ API/CLI transports**, so existing error-handling code keeps working:
 | `already have such address` / already-exists | `TikAlreadyHaveSuchItemException` |
 | object doesn't exist / unresolvable `.id` | `TikNoSuchItemException` |
 | unmapped path / missing plugin | `TikNoSuchCommandException` |
-| other M2 errors | `TikCommandUnexpectedResponseException` (message includes the router's error string) |
+| other M2 errors | `TikCommandTrapException` (message includes the router's error string) — the same generic fallback as the API/CLI/REST transports |
 
 ## Capability
 
-CRUD **and Listen** — native reports `Crud | Listen`, so the callback-based async APIs work in addition to
-the synchronous CRUD ones:
+CRUD, **Listen** and **Safe Mode** — native reports `Crud | Listen | SafeMode`, so the callback-based async
+APIs work in addition to the synchronous CRUD ones, and `SafeModeTake()` / `SafeModeRelease()` are available
+via the native M2 `toggleSafeMode` commands (**no in-place `SafeModeUnroll()`** — to roll back, drop the
+connection without releasing; see [Safe Mode](Safe-Mode)):
 
 ```cs
-bool hasCrud   = conn.Supports(TikConnectionCapability.Crud);   // true
-bool hasListen = conn.Supports(TikConnectionCapability.Listen); // true
+bool hasCrud     = conn.Supports(TikConnectionCapability.Crud);     // true
+bool hasListen   = conn.Supports(TikConnectionCapability.Listen);   // true
+bool hasSafeMode = conn.Supports(TikConnectionCapability.SafeMode); // true
 ```
 
 `ExecuteAsync` / `LoadAsync` / `LoadListenAsync` are supported and cover three shapes:
diff --git a/login-versions.md b/login-versions.md
index 643d4c2..dad9041 100644
--- a/login-versions.md
+++ b/login-versions.md
@@ -31,7 +31,7 @@ using (var connection = ConnectionFactory.CreateConnection(TikConnectionType.Api
 ## Why `Api_v2` / `ApiSsl_v2` are obsolete
 
 Earlier tik4net versions exposed separate `Api_v2` / `ApiSsl_v2` constants for the 6.43+ login. Since the
-login process was unified (RouterOS 3.5.0 era — see [History](History)), those constants are **obsolete and
+login process was unified in **tik4net 3.5.0** (see [History](History)), those constants are **obsolete and
 removed from use** — `Api` / `ApiSsl` handle both. Replace any `Api_v2` / `ApiSsl_v2` in old code with
 `Api` / `ApiSsl`.
 
-- 
2.43.0

